# KRX 변동성 드래그 스크리너 — 개발 로드맵

## 0. 프로젝트 정의

### 문제 의식
기존 퀀트 플랫폼의 `OptionsPricerCard.tsx`는 이미 Black–Scholes 가격과 그릭스를 출력한다.
따라서 옵션 가격 계산을 반복하는 것은 중복이다. 본 프로젝트는 설문 선택지 중
**"Pivot off options → cross-universe variance-drain leaderboard"** 방향을 채택하되,
대상 시장을 **한국거래소(KRX)** 로 특정하여 기존 기능과 완전히 분리되는 산출물을 만든다.

### 한 문장 정의
> KRX 전 종목의 로그수익률로부터 Itô 보정항 `½σ²` 을 추정하여,
> **산술 드리프트 μ 와 기하 성장률 g = μ − ½σ² 의 괴리(변동성 드래그)** 를
> 횡단면으로 순위화하는 스크리너.

### 왜 이것이 "새로운" 기능인가
| 기존 기능 | 본 프로젝트 |
|---|---|
| 단일 종목 · 옵션 가격 | 전 종목 횡단면 · 주식 수익률 |
| σ 를 **입력**으로 받음 | σ 를 실현 데이터에서 **추정** |
| 위험중립 측도 (drift = r) | 실세계 측도 (drift = μ) |
| 가격 산출 | 순위 · 진단 산출 |

---

## 1. 수학적 기반 (구현 전 확정 사항)

기하 브라운 운동 가정:

```
dS_t = μ S_t dt + σ S_t dB_t
```

Itô의 보조정리를 `f(S) = ln S` 에 적용하면

```
d ln S_t = (μ − ½σ²) dt + σ dB_t
```

따라서 `ln(S_T/S_0) ~ N((μ − ½σ²)T, σ²T)` 이고, **핵심 정의**는 다음과 같다.

| 기호 | 의미 | 추정식 (일간 로그수익률 r_i, 연율화 계수 A=252) |
|---|---|---|
| `g` | 기하 성장률 (로그 드리프트) | `ĝ = A · r̄` |
| `σ²` | 순간 분산 | `σ̂² = A · s²` (s²: 표본분산, ddof=1) |
| `μ` | 산술 드리프트 | `μ̂ = ĝ + ½σ̂²` |
| `D` | **변동성 드래그** | `D = ½σ̂² = μ̂ − ĝ` |
| `ρ` | 드래그 비율 | `ρ = D / μ̂` (μ̂ > 0 일 때만 정의) |

### 반드시 지켜야 할 검증 항목
1. **항등식**: `μ̂ − ĝ = ½σ̂²` 가 부동소수점 오차 내에서 정확히 성립할 것.
2. **일치성**: 알려진 (μ, σ) 로 GBM을 시뮬레이션했을 때 추정량이 신뢰구간 안에서 복원될 것.
3. **교차검증**: `μ̂` 은 단순수익률의 연율화 평균과 근사해야 함.
4. **이차변동**: 실현 이차변동 `(A/n)·Σr_i²` 이 `σ̂²` 과 근사해야 함 (Itô: `(dB)²=dt`).

### 불확실성 정량화
- `SE(ĝ) = σ̂ · √(A/n)`
- `SE(σ̂²) = σ̂² · √(2/(n−1))` (정규성 가정) → `SE(D) = σ̂² / √(2(n−1))`
- `σ²` 신뢰구간은 카이제곱 분포로 산출.

### 가정 위반 진단 (정직성 확보)
GBM은 iid 정규를 가정하나 실제 주가는 이를 위반한다. 다음을 **반드시 함께 보고**한다.
- 초과첨도 (fat tail)
- 왜도
- Jarque–Bera 정규성 검정
- 제곱수익률에 대한 Ljung–Box 검정 (변동성 군집)
- 유효표본크기 보정

---

## 2. 데이터 파이프라인

### 유니버스
- **1차**: KRX KIND 상장법인목록 (`kind.krx.co.kr/corpgeneral/corpList.do`) — 실시간·공식
- 6자리 숫자 코드만 채택 (우선주·신규상장 임시코드 제외)
- 캐시: `data/cache/universe_{date}.parquet`

### 가격
- **1차**: `yfinance` (`{code}.KS` / `{code}.KQ`, `auto_adjust=True` → 배당·액면분할 조정)
- **2차**: `pykrx` (KRX 원본 OHLCV, 미조정)
- 배치 다운로드 + 로컬 parquet 캐시로 재현성 확보

### 필터
- 최소 관측일수 (기본 250거래일)
- 최소 중위 거래대금 (유동성) — 시가총액 API 부재를 우회
- 상장폐지·거래정지 종목 제거

---

## 3. 단계별 구현 계획

### v0.1 — 기본 스크리너 (완료)

| 단계 | 산출물 | 검증 기준 | 상태 |
|---|---|---|---|
| **P0** 저장소 골격 | git init, 라이선스, .gitignore, 의존성 | `pip install -e .` 성공 | ✅ |
| **P1** 수학 코어 | `metrics.py` | 합성 GBM 테스트 통과 | ✅ |
| **P2** 진단 | `diagnostics.py` | 정규분포 입력 시 JB 기각 안 함 | ✅ |
| **P3** 데이터 계층 | `universe.py`, `data.py` | 실제 종목 다운로드·캐시 성공 | ✅ |
| **P4** 스크리너 | `screener.py` | 전 종목 랭킹 산출 | ✅ |
| **P5** 리포트 | `report.py` | Markdown + CSV + 차트 | ✅ |
| **P6** CLI | `cli.py` | `krxdrag` 동작 | ✅ |
| **P7** CI | GitHub Actions | 푸시 시 테스트 자동 실행 | ✅ |
| **P8** 배포 | README, 실제 실행 결과 | GitHub 업로드 | ✅ |

### v0.2 — 드래그를 네 방향으로 확장 (완료)

단일 스냅샷 순위를 **시간축 · 횡단면 구조 · 배율 민감도 · 성분 분해**로 넓힌 릴리스.

| 단계 | 산출물 | 검증 기준 | 상태 |
|---|---|---|---|
| **P9** 오프라인 테스트 하네스 | `tests/conftest.py`, `test_screener.py`, `test_report.py` | 네트워크 없이 `screen()` 종단 실행 | ✅ |
| **P10** 점프 분해 | `jumps.py` | 점프 주입 경로에서 점프 분산 복원, 무점프 경로에서 BNS 검정 크기 유지 | ✅ |
| **P11** 롤링 드래그 | `rolling.py` | 전체 길이 창 = `compute_drag()` (1e-12), 레짐 전환 추적 | ✅ |
| **P12** 섹터 집계 | `sectors.py` | 포트폴리오 드래그 < 구성종목 중위 드래그 | ✅ |
| **P13** 레버리지 | `leverage.py` | 일간 리밸런싱 경로가 `g_L = L·g − (L²−L)·D` 복원 | ✅ |
| **P14** 리포트·CLI 통합 | `report.write_html()`, 신규 플래그 | HTML 외부 참조 0건, 종단 CLI 실행 | ✅ |
| **P15** 문서·버전 | README, ROADMAP, `0.2.0` | — | ✅ |

**v0.2 검증 요약**: 테스트 149개, 커버리지 90%. 미커버 구간은 네트워크가 필요한
다운로드 경로(`data.py`, `universe.py`)로, CI에서 실행 불가하다.

#### 이번 릴리스에서 함께 고친 결함

| 위치 | 문제 |
|---|---|
| `screener.py` | Itô 항등식 확인이 bare `assert` — `python -O` 에서 소멸 |
| `data.to_wide()` | `(date, symbol)` 중복 시 `pivot()` 예외로 전체 런 중단 |
| `report.py` | 파일명이 `date.today()` 하드코딩 — 테스트·재현 불가 |
| `sectors.py` | 포트폴리오 수준 시계열 재구성 시 첫 수익률 유실 (개발 중 발견) |
| `report.py` (HTML) | `text-transform: uppercase` 가 σ→Σ, μ→M 로 표기 파괴 |

### v0.3 — 추정의 질 (계획)

v0.1은 드래그를 **계산**했고, v0.2는 그것을 네 방향으로 **쪼갰다**.
두 릴리스 모두 `½σ̂²` 이라는 숫자 자체가 얼마나 믿을 만한지는 다루지 않았다.
v0.3의 주제는 그것이다 — **숫자를 더 정확하게 만들고, 이 프로젝트가 스스로 주장해 온
명제를 실제로 측정한다.**

#### 왜 이 순서인가

README와 §5는 창립 이래 같은 주장을 반복해 왔다:

> "μ̂ 의 표준오차는 매우 크다. σ̂² 은 상대적으로 정확하므로 드래그 ½σ̂² 자체는 μ̂ 보다
> 신뢰할 만하다."

이것은 **한 번도 측정된 적이 없다.** 문헌상 옳을 가능성이 높지만, 이 저장소의 데이터로
확인된 바는 없고 리포트 어디에도 그 근거가 없다. 정직성을 표방하는 도구가 자기 핵심
전제를 검증하지 않은 채 두는 것은 일관성이 없다. 그래서 P17이 이 릴리스의 중심이고,
P16은 그 전에 추정량 자체를 개선해 두는 단계다.

| 단계 | 산출물 | 검증 기준 | 상태 |
|---|---|---|---|
| **P16** 범위 기반 변동성 | `volatility.py`, OHLC 데이터 계층 | 시뮬레이션에서 불편성 + 이론 효율비 달성, 드리프트 하에서 RS 무편향 | ✅ |
| **P17** 추정량 지속성 검증 | `validation.py` | 이질적 (μᵢ, σᵢ) 합성 패널에서 σ̂ 지속성 ≫ μ̂ 지속성, 표준오차가 예측하는 크기와 일치 | ✅ |
| **P18** 다중검정 보정 | `diagnostics.py` 확장 | 1,000개 귀무 계열에서 FDR ≤ q, 혼합 계열에서 검정력 확보 | |
| **P19** 결과 저장소 | `store.py`, `krxdrag history` | 반복 실행이 누적되고 종목별 시계열 조회 가능 | |
| **P20** 2차 소스·생존 편의 | `data.py` 폴백, 유니버스 아카이브 | yfinance 실패 시 pykrx 로 폴백, 스냅샷 누적 시작 | |
| **P21** React 리더보드 카드 | `web/` 컴포넌트 | 기존 플랫폼에 편입 | |

#### P16 — 범위 기반 변동성 추정량

모든 산출물이 `σ̂²` 의 함수인데, 현재는 **가장 비효율적인 추정량**(종가 대 종가 표본분산)을
쓰고 있다. 일중 고가·저가를 쓰는 추정량은 같은 데이터에서 훨씬 정확하다.

| 추정량 | 식 | 드리프트 |
|---|---|---|
| Close-to-close | `var(r, ddof=1)` | 무관 |
| Parkinson | `(1/4ln2)·(ln(H/L))²` | **0 가정** |
| Garman–Klass | `½(ln(H/L))² − (2ln2−1)(ln(C/O))²` | **0 가정** |
| Rogers–Satchell | `ln(H/C)ln(H/O) + ln(L/C)ln(L/O)` | **무편향** |
| Yang–Zhang | 오버나이트 + 시가-종가 + RS 결합 | 무편향, 갭 처리 |

효율과 드리프트 민감도를 **인용하지 않고 직접 시뮬레이션으로 측정했다**
(일중 GBM 을 촘촘히 생성해 O/H/L/C 로 축약, σ=40%).

일간 추정량 분산 기준 효율: **Parkinson 5.2× · Garman–Klass 8.2× · Rogers–Satchell 6.4×**.
문헌값(5 / 7.4 / 6)과 부합한다.

드리프트를 올렸을 때의 편의 (촘촘한 표본, 참값 대비 %):

| 연 드리프트 | Parkinson | Garman–Klass | Rogers–Satchell |
|---|---|---|---|
| 0% | −2.6% | −3.0% | −2.7% |
| 100% | −1.8% | −2.7% | −2.7% |
| 300% | **+5.5%** | −0.1% | −2.8% |
| 600% | **+30.2%** | +8.6% | −3.2% |

> **구현 결과 (P16 완료): 기본값은 Yang–Zhang 이 되었다.** 계획 단계에서는
> Rogers–Satchell 을 기본으로 두려 했으나, 구현 중 측정해 보니 **오버나이트 갭**이
> 결정적이었다. Parkinson · Garman–Klass · Rogers–Satchell 은 모두 하나의 봉 안에서만
> 계산되므로 종가에서 다음 시가로 건너뛰는 움직임을 보지 못한다. 변동성의 40%가 갭에
> 있는 합성 데이터에서 이들은 σ² 를 **약 42% 과소평가**했고, 종가 대비 추정량과
> Yang–Zhang 만 참값을 복원했다. 한국 주식은 서울장이 닫힌 사이 해외 시장이 움직이므로
> 이는 반올림 오차가 아니다. Yang–Zhang 은 드리프트에 강건하면서 갭 항을 명시적으로
> 갖는 유일한 추정량이라 `DEFAULT_METHOD` 가 되었다.

**Rogers–Satchell 과 Yang–Zhang 이 이 프로젝트에 맞는 선택이다.** Parkinson 과
Garman–Klass 는 드리프트가 0이라고 가정하는데, 이 프로젝트는 **드리프트와 분산의
상호작용 그 자체**를 다룬다. 위 표에서 RS 만 드리프트에 대해 편의가 평평하다.
더 중요한 것은 **Parkinson 의 편의가 μ 와 함께 증가한다**는 점이다 — σ² 추정 오차가
μ 와 상관되면 드래그와 μ 의 관계 자체가 오염된다. 이 저장소의 2026-08-16 스냅샷에도
μ 가 130~196% 인 종목이 실제로 존재하므로 가정상의 우려가 아니다.
네 가지를 모두 구현하되 기본값은 드리프트에 강건한 쪽으로 둔다.

> **주의 1 — 이산 표본 편의.** 범위 기반 추정량은 **모두 하방 편의**를 갖는다. 연속시간의
> 진짜 고·저가는 관측되지 않고, 체결이 성길수록 관측 범위가 좁아지기 때문이다.
> 측정값(일중 스텝 수별 편의):
>
> | 일중 스텝 | Parkinson | Garman–Klass | Rogers–Satchell |
> |---|---|---|---|
> | 50 | −16.9% | −23.6% | −23.9% |
> | 200 | −8.2% | −12.0% | −12.0% |
> | 3200 | −2.6% | −3.0% | −2.7% |
>
> 이 편의는 **유동성과 상관된다.** 거래가 적은 종목일수록 하방 편의가 크므로,
> 1,100 종목 횡단면에서 저유동성 종목의 드래그가 체계적으로 과소평가된다.
> 효율 이득만 보고 도입하면 **횡단면 순위를 유동성 방향으로 왜곡**하게 된다.
> 종가 대비 추정량과 나란히 보고하고, 유동성 구간별 괴리를 함께 실어야 한다.

> **주의 2 — 한국 시장 가격제한폭.** KRX 는 일간 제한폭이 ±30% 다. 상하한가에 걸린 날은
> 고가·저가가 잘려 추가적인 하방 편의가 생긴다. 제한폭 도달일을 표시하고 그 비율을 함께
> 보고해야 한다 — 감추면 v0.2 의 GBM 진단과 같은 성격의 정직성 문제가 된다.

데이터 계층은 현재 Close 와 Volume 만 받는다(`data._download_batch`). Open/High/Low 를
함께 받도록 확장해야 하며, `auto_adjust=True` 는 OHLC 를 일관되게 조정하므로 그대로 쓴다.

#### P17 — 이 프로젝트의 핵심 주장을 측정한다

설계: 이력을 길이 W 의 비중첩 창으로 나누고, 각 종목에 대해 창 t 에서 추정한
`σ̂²_t, ĝ_t, μ̂_t` 가 창 t+1 에서 **실현된** 값을 얼마나 예측하는지 잰다.
횡단면 Spearman 상관과 표본외 R² 를 추정량별로 보고한다.

오프라인 검증이 가능한 이유: 이질적이지만 **시간에 대해 고정된** `(μᵢ, σᵢ)` 로 합성 패널을
만들면, `σ̂` 는 t 와 t+1 에서 같은 참값을 추정하므로 순위상관이 높아야 하고, `μ̂` 는 추정
오차가 참값 분산을 압도하므로 상관이 0 근처여야 한다. **그 격차의 크기까지 표준오차로
예측된다** — 재현 여부가 명확한 검증 기준이다.

결과가 어느 쪽으로 나오든 리포트에 싣는다. 만약 σ̂ 의 지속성마저 낮게 나온다면 그것은
드래그 순위 전체의 해석을 바꾸는 발견이며, 감출 이유가 없다.

> **구현 결과 (P17 완료).** 참 모수를 시간에 대해 고정한 합성 패널에서 측정한 결과,
> 주장은 **성립하고 그 크기까지 이론과 맞았다.**
>
> | 추정량 | 순위 지속성 ρ | 표본외 R² | 잡음 상한 |
> |---|---|---|---|
> | σ² (= 드래그) | **0.97** | +0.89 | 0.95 |
> | g | 0.12 | −0.81 | 0.14 |
> | μ | **0.10** | **−0.85** | 0.11 |
>
> 해석적 예측(`Var(신호)/(Var(신호)+Var(잡음))`)은 σ² 0.950 · μ 0.072 였고,
> 측정된 Pearson 은 0.946 · 0.070 이었다. **μ 의 표본외 R² 가 음수**라는 것은 직전 창의
> μ̂ 을 예측으로 쓰는 것이 횡단면 평균을 그냥 찍는 것보다 나쁘다는 뜻이다 — 순위가 약한
> 정도가 아니라 정보가 음이다.
>
> 대조군도 함께 확인했다. 모든 종목이 **같은** 참 σ 를 갖는 패널에서는 σ² 지속성이
> 0.97 → **0.04** 로 붕괴한다. 즉 이 지표는 «분산은 원래 재기 쉽다»가 아니라 실제
> 횡단면 신호를 재고 있다.

#### P18 — 다중검정 보정

v0.2 는 1,106 종목에 JB · Ljung–Box · BNS 세 검정을 각각 5% 유의수준으로 적용하고
통과 비율을 보고한다. 1,106 회 검정에서 **우연히 55건 내외의 거짓 양성**이 나온다.
`has_jumps` 가 "유의한 점프"라고 말할 때 그 중 상당수가 잡음일 수 있다.
Benjamini–Hochberg FDR 을 적용해 원시 기각 수와 보정 후 기각 수를 함께 싣는다.

---

## 4. 더 뒤로 미룬 것

- **Merton jump-diffusion MLE**: v0.2는 이중멱변동으로 점프 *분산*만 분해한다.
  `(λ, μ_J, σ_J)` 모수 추정은 별개 작업이며 수렴이 불안정하다.
- **일중 데이터**: 점프 분해의 점근이론은 일간보다 촘촘한 격자를 상정한다.
  P16 의 범위 기반 추정량이 일중 정보를 부분적으로만 대체한다.
- **생존 편의의 완전한 정량화**: P20 은 유니버스 스냅샷 **누적을 시작**할 뿐이다.
  실제 정량화는 충분한 이력이 쌓인 뒤에야 가능하다 — 지금 할 수 있다고 말하면 거짓이다.

---

## 5. 명시적 한계

1. `μ̂` 의 표준오차는 매우 크다. 드리프트 추정은 원래 어렵다 — **순위를 투자 신호로 해석하면 안 된다.**
2. `σ̂²` 은 상대적으로 정확하므로, 드래그 `½σ̂²` 자체는 `μ̂` 보다 신뢰할 만하다.
   — **P17 에서 측정 완료**: 순위 지속성 σ² 0.97 대 μ 0.10, μ 의 표본외 R² 는 −0.85.
   드래그는 `σ²` 의 단조 변환이므로 두 주장은 하나다.
3. GBM 가정(정규성·독립성)은 대부분의 종목에서 기각된다. 진단 지표를 함께 볼 것.
4. 과거 실현 변동성이지 미래 예측이 아니다.
5. **업종 중위 드래그는 업종 포트폴리오의 드래그가 아니다.** 포트폴리오 분산은 상관이
   1보다 작은 만큼 감쇠하므로 항상 더 작다. v0.2는 두 값을 나란히 보고한다.
6. 점프 분해(이중멱변동)의 점근이론은 일간보다 촘촘한 격자를 상정한다.
   분산이 어디서 오는지에 대한 지표로 읽되 정밀한 측정값으로 보지 말 것.
7. 레버리지 ETF 상품 테이블의 코드·배율은 KRX 원본 확인이 필요하다.
   코드는 이를 신뢰하지 않고 실측 회귀로 배율을 검증해 불일치를 보고한다.
8. **본 도구는 교육·연구 목적이며 투자 자문이 아니다.**
